How to launch the system:
1. Prepare the database:
	1.1 Create a postgres server with a datebase for the app
	1.2 Run the script that creates the tables and the script that fills the tables with initial date. These script are located at the RESTserver
2. Prepare the server
	2.1 Fill the Settings.XML in the REST server with the database data (username, password, db name and adress)
	2.2 Run the server
3. Prepare the client
	3.1 Aviv will write this